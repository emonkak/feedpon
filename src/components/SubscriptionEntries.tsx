import * as React from 'react';

export default class SubscriptionEntries extends React.PureComponent<any, any> {
    render() {
        return (
            <div className="main">
                <h1>Subscription feeds are available here</h1>
            </div>
        );
    }
}
