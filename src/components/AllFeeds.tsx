import * as React from 'react';

export default class AllFeeds extends React.PureComponent<any, any> {
    render() {
        return (
            <p>All feeds are available here.</p>
        );
    }
}