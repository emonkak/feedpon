import * as React from 'react';

export default class SubscriptionFeeds extends React.PureComponent<any, any> {
    render() {
        return (
            <p>Subscription feeds are available here.</p>
        );
    }
}