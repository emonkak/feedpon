import { UI } from 'messaging/types';

const ui: UI = {
    customStyles: '',
    helpIsOpened: false,
    isScrolling: false,
    selectedStreamId: '',
    sidebarIsOpened: true,
    theme: 'theme-light',
    version: 1
};

export default ui;
