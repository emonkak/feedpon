import { State } from 'messaging/types';

const initialState: State = {
    entries: [],
    feed: null,
    notifications: [],
    pageTitle: 'Feedpon',
    subscriptions: [],
};

export default initialState;
