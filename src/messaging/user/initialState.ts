import { User } from 'messaging/types';

const user: User = {
    isLoaded: false,
    isLoading: false,
    profile: {
        userName: '<unknown>',
        source: '<unknown>',
        picture: ''
    },
    version: 1
};

export default user;
