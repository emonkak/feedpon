{
  "type": "module",
  "scripts": {
    "build": "wireit",
    "ci": "pnpm run clean && pnpm run lint && pnpm run build && pnpm test --coverage",
    "clean": "rm --force --recursive dist",
    "format": "biome check --fix",
    "lint": "biome ci .",
    "test": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.10",
    "esbuild": "^0.28.1",
    "esbuild-plugin-minify-templates": "github:emonkak/barebind#path:tools/esbuild-plugin-minify-templates",
    "fast-check": "^4.9.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wireit": "^0.14.13"
  },
  "wireit": {
    "build": {
      "command": "node --import tsx build.ts",
      "files": [
        "app/css/**/*",
        "build.ts"
      ],
      "output": [
        "dist/app.css",
        "dist/app.js",
        "dist/background.js"
      ],
      "dependencies": [
        "dist:/",
        "dist:/img",
        "typecheck"
      ]
    },
    "dist:/": {
      "command": "install -D --mode=644 --target-directory=dist app/{app.html,manifest.json}",
      "files": [
        "app/app.html",
        "app/manifest.json"
      ],
      "output": [
        "dist/app.html",
        "dist/manifest.json"
      ]
    },
    "dist:/img": {
      "command": "install -D --mode=644 --target-directory=dist/img app/img/*",
      "files": [
        "app/img/*"
      ],
      "output": [
        "dist/img/*"
      ]
    },
    "typecheck": {
      "command": "tsc --build --pretty --incremental",
      "clean": "if-file-deleted",
      "files": [
        "app/src/**/*",
        "packages/*/src/**/*",
        "pnpm-lock.yaml",
        "tsconfig.base.json",
        "tsconfig.json"
      ],
      "output": [
        "packages/*/*.tsbuildinfo"
      ]
    }
  }
}
