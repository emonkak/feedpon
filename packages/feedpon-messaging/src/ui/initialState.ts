import type { UI } from '../types.ts';

const ui: UI = {
  customStyles: '',
  helpIsOpened: false,
  selectedStreamId: '',
  sidebarIsOpened: true,
  theme: 'theme-light',
  version: 1,
};

export default ui;
